# WhereMusic 설계문서

> YouTube 재생 위치에 맞춰 악보 이미지 위 포인터를 동기화하는 웹 애플리케이션

---

## 1. 개요

### 1.1 목적
사용자가 YouTube 영상(음악)을 재생하면서, 업로드한 악보 이미지에서 **현재 연주 중인 위치**를 포인터로 표시한다. 연습·합주·독보 시 “지금 어디를 연주하는지”를 한눈에 파악하는 것이 목표다.

### 1.2 핵심 시나리오
1. YouTube URL을 입력하고 영상을 로드한다.
2. 악보 이미지(1장 이상)를 업로드한다.
3. 악보 위 **구간(타임라인 매핑)** 을 지정한다. (예: 0:00~0:32 → 1단, 0:32~1:10 → 2단)
4. 재생 시 현재 시각에 해당하는 악보 영역으로 포인터가 이동한다.

### 1.3 범위 (MVP)
| 포함 | 제외 (후속) |
|------|-------------|
| YouTube URL 재생 | 자동 악보 인식(OMR) |
| 악보 이미지 업로드·표시 | MIDI/Audio 피치 추적 |
| 수동 구간–시간 매핑 | 다중 사용자 실시간 공유 |
| 재생 동기 포인터 | 모바일 네이티브 앱 |

---

## 2. 화면 구성

PC 기준 **좌측 YouTube / 우측 악보** 2단 배치.

```
┌──────────────────────────────────────────────────────────┐
│  WhereMusic          [제목] [저장] [불러오기] …            │
├────────────────────────────┬─────────────────────────────┤
│  YouTube URL [____] [불러오기] │  [업로드] [매핑 편집]        │
│  ┌──────────────────────┐  │  ┌───────────────────────┐  │
│  │   YouTube Player     │  │  │  악보 이미지 + 포인터 ● │  │
│  └──────────────────────┘  │  └───────────────────────┘  │
│                            │  ◀ 1/3 ▶  / 구간 목록         │
└────────────────────────────┴─────────────────────────────┘
```

### 2.1 YouTube 영역
- URL 입력 → videoId 추출 → IFrame Player API로 임베드
- 재생/일시정지/시크는 플레이어 컨트롤 사용
- 현재 재생 시각(`currentTime`)을 주기적으로 읽어 동기화에 사용

### 2.2 악보 영역
- 드래그앤드롭 또는 파일 선택으로 이미지 업로드 (PNG/JPG/WebP)
- 여러 장일 경우 페이지 전환
- 포인터: 세로/가로 막대 또는 원형 하이라이트 (설정 가능)
- 매핑 편집 모드: 사용자가 악보 위 사각형(ROI)과 시작·종료 시각을 연결

---

## 3. 동기화 모델

### 3.1 기본 아이디어
악보를 “시간 축에 매핑된 영역”의 나열로 본다.

```
Segment {
  id, pageIndex,
  startSec, endSec,          // YouTube 시간
  x, y, width, height,       // 이미지 정규화 좌표 (0~1)
  pathType: "line" | "box"   // 포인터 이동 방식
}
```

재생 시각 `t`에 대해:
1. `startSec ≤ t < endSec` 인 Segment를 찾는다.
2. Segment 내부 진행률 `p = (t - startSec) / (endSec - startSec)`
3. `pathType === "line"`이면 좌→우(또는 단 방향)로 포인터 x를 보간  
   `pathType === "box"`이면 해당 영역을 고정 하이라이트

### 3.2 매핑 UX (수동)
1. 재생을 원하는 지점에서 일시정지
2. “구간 시작” 클릭 → 악보에서 ROI 드래그
3. 재생 후 “구간 종료” 시각 지정
4. 반복하여 전곡(또는 연습 구간) 매핑

> MVP는 **수동 매핑**만 지원한다. 자동 추적(오디오 분석)은 정확도·비용 이슈로 후순위로 둔다.

### 3.3 동기화 루프
- `requestAnimationFrame` 또는 100~250ms 폴링으로 `player.getCurrentTime()` 조회
- Segment 탐색은 정렬된 배열 + 이진 탐색 (또는 현재 인덱스 캐시)
- 시크/점프 시 인덱스 재탐색

---

## 4. 시스템 구조

```
[Browser]
  UI (React)
    ├─ YoutubePlayer   ← YouTube IFrame API
    ├─ ScoreViewer     ← Canvas/SVG overlay
    ├─ MappingEditor   ← ROI + time binding
    └─ SyncEngine      ← time → pointer position
  Local Storage / IndexedDB
    └─ project: { videoId, images[], segments[] }
```

- **프론트 단독 MVP**: 서버 없이 로컬 저장으로 프로젝트 유지
- 이미지는 IndexedDB(또는 Object URL)에 보관, 메타데이터는 JSON

---

## 5. 주요 기능 명세

| ID | 기능 | 설명 |
|----|------|------|
| F01 | URL 로드 | `youtu.be`, `youtube.com/watch` 파싱, 유효성 검사 |
| F02 | 재생 제어 | play / pause / seek 상태 반영 |
| F03 | 악보 업로드 | 다중 이미지, 순서 변경, 삭제 |
| F04 | 구간 매핑 | ROI + start/end 초 단위 저장·수정·삭제 |
| F05 | 포인터 동기 | 재생 중 실시간 위치 갱신, 페이지 자동 전환 |
| F06 | 프로젝트 저장 | 로컬 저장/불러오기 (이름 지정) |
| F07 | 내보내기 | 매핑 JSON 다운로드 (이미지 제외 또는 zip) |

---

## 6. 기술 스택 (권장)

| 구분 | 선택 |
|------|------|
| UI | React + TypeScript + Vite |
| 스타일 | CSS Modules 또는 Tailwind |
| 영상 | YouTube IFrame Player API |
| 악보 오버레이 | HTML Canvas 또는 SVG |
| 저장 | IndexedDB (`idb` 등) |
| 배포 | 정적 호스팅 (Vercel/Netlify 등) |

---

## 7. 데이터 스키마 (요약)

```json
{
  "id": "proj_xxx",
  "title": "연습곡 A",
  "videoId": "dQw4w9wgwc",
  "pages": [
    { "id": "p1", "name": "page1.png", "width": 2480, "height": 3508 }
  ],
  "segments": [
    {
      "id": "s1",
      "pageId": "p1",
      "startSec": 0,
      "endSec": 32.5,
      "rect": { "x": 0.08, "y": 0.12, "w": 0.84, "h": 0.08 },
      "pathType": "line"
    }
  ]
}
```

좌표는 이미지 해상도에 독립적인 **정규화 비율**로 저장한다.

---

## 8. 비기능 요구사항

- **반응성**: 포인터 갱신 지연 체감 ≤ 100ms (폴링 주기·렌더 최적화)
- **호환**: 최신 Chrome/Edge/Safari 데스크톱 우선
- **용량**: 악보 이미지 장당 권장 ≤ 5MB, 프로젝트당 이미지 합 ≤ 50MB
- **개인정보**: YouTube/악보는 사용자 기기 로컬 처리, 외부 전송 없음 (MVP)
- **접근성**: 키보드로 재생·구간 이동 가능하면 가점

---

## 9. 리스크 및 대응

| 리스크 | 대응 |
|--------|------|
| YouTube API 제한·광고·지역 차단 | 명확한 오류 메시지, URL 재입력 유도 |
| 매핑 작업 부담 | 복제/분할, 템플릿 단 높이 스냅 |
| 이미지 확대 시 포인터 오차 | 정규화 좌표 + 뷰포트 변환 일원화 |
| 긴 곡의 Segment 수 증가 | 이진 탐색, 가상 스크롤(편집 목록) |

---

## 10. 개발 단계

| Phase | 내용 | 산출물 |
|-------|------|--------|
| P1 | YouTube 재생 + 악보 표시 | 기본 화면 |
| P2 | 수동 매핑 에디터 | Segment CRUD |
| P3 | SyncEngine + 포인터 | 동기 재생 |
| P4 | 로컬 저장/불러오기 | 프로젝트 지속성 |
| P5 | UX  refinement | 단축키, 스냅, 내보내기 |

---

## 11. 성공 기준 (MVP)

- URL 하나로 영상 재생이 된다.
- 악보 이미지를 올리고 2개 이상 구간을 매핑할 수 있다.
- 재생·일시정지·시크 시 포인터가 해당 구간/위치로 맞게 움직인다.
- 새로고침 후에도 프로젝트를 다시 불러올 수 있다.

---

*문서 버전: 0.1 · 대상: WhereMusic MVP*
